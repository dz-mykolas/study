#include <stdio.h>
#include <stdlib.h>

#include "llist.h"

struct Person *createNode()

void llist_add_end(struct Person **list, char *name, char *surname, char *email, char *phone)
{
    
}

void llist_add_at(struct Person **list, char *name, char *surname, char *email, char *phone)
{
    
}